#ifndef PLEX_STANDARDSIZES_H
#define PLEX_STANDARDSIZES_H

// This is a header that contains a set of standard numbers that aren't quite limits. As with all headers in the meta folder, there is no correlating source file.

// NOTE: sometime in the future this header might be fully merged with something else.

// Standard dimensions for bitmap fonts essentially 1/16th width by 1/8th height. Optional but useful for printing from bitmap fonts:

#define PLEX_STANDARD_FW 0.0625
#define PLEX_STANDARD_FH 0.125

#endif
